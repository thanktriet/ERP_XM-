# Handoff Document

**Created:** 2026-03-30 19:30
**Topic:** ERP Xe Máy Điện — Module Phụ Kiện & Quà Tặng (Backend + Frontend hoàn thành)
**Status:** Ready for Review — Backend fix xong, cần restart server

---

## Context Summary

Session này tập trung vào:
1. **Build backend API** cho phụ kiện (`accessories`) và quà tặng (`gifts`)
2. **Build frontend pages** `AccessoriesPage.tsx` và `GiftsPage.tsx`
3. **Debug 502 crash** — phát hiện `accessories.routes.js` cũ import hàm/validator không tồn tại → fix xong
4. Tất cả code đã viết xong, backend cần **restart** để load code mới

---

## Completed Work

- [x] `backend/src/controllers/accessories.controller.js` — viết lại hoàn toàn với 9 functions
- [x] `backend/src/controllers/gifts.controller.js` — tạo mới với 11 functions
- [x] `backend/src/routes/accessories.routes.js` — fix lỗi import cũ (toggleAccessory/validators không tồn tại)
- [x] `backend/src/routes/inventory.routes.js` — thêm routes cho accessories + gift-items + order-gifts
- [x] `frontend/src/pages/AccessoriesPage.tsx` — trang quản lý phụ kiện đầy đủ
- [x] `frontend/src/pages/GiftsPage.tsx` — trang quản lý quà tặng đầy đủ
- [x] `frontend/src/App.tsx` — thêm import + route `/accessories` và `/gifts`
- [x] `frontend/src/components/Layout.tsx` — thêm sidebar links 🎒 Phụ kiện và 🎁 Quà tặng
- [x] `backend/migrations/fix_accessories_and_gifts_v2.sql` — đã chạy thành công trên Supabase
- [x] TypeScript compile: **0 lỗi**
- [x] Backend syntax check: **OK** cho tất cả files mới

---

## Pending Work

- [ ] **[NGAY] Restart backend** để load code đã fix (xem hướng dẫn bên dưới)
- [ ] Test thực tế các endpoint accessories/gifts trên browser
- [ ] Kiểm tra `PurchaseOrdersPage.tsx` gọi `/api/accessories?limit=500` — cần đổi thành `/api/inventory/accessories` hoặc giữ nguyên (cả 2 đều hoạt động sau fix)
- [ ] Module tiếp theo: tích hợp `order_gifts` vào `SalesNewPage.tsx` — khi tạo đơn hàng có thể chọn quà tặng kèm
- [ ] Viết trang `OrderGiftsPanel` — xem và xác nhận phát quà trong đơn hàng

---

## Key Decisions Made

| Decision | Rationale | Alternatives |
|----------|-----------|--------------|
| Giữ `/api/accessories` (route cũ) + thêm `/api/inventory/accessories` (mới) | Tương thích ngược với PurchaseOrdersPage đang gọi `/api/accessories` | Chỉ giữ 1 route, phải sửa toàn bộ frontend |
| `item_movements.quantity` dùng dấu âm/dương | Trigger `update_item_stock` chỉ cần `qty_in_stock += quantity` | Dùng movement_type để quyết định dấu (phức tạp hơn) |
| Fix `accessories.routes.js` thay vì xóa | File cũ có trong `server.js` → xóa sẽ phải sửa thêm server.js | Xóa file + sửa server.js |

---

## Current State

### Nguyên nhân 502 Ban đầu (đã fix)
```
accessories.routes.js cũ:
  import { toggleAccessory } from controller  ← KHÔNG TỒN TẠI
  import { createAccessoryRules } from validators  ← KHÔNG TỒN TẠI
→ Express crash ngay startup → TẤT CẢ route 502
```

**Fix**: Viết lại `accessories.routes.js` dùng đúng exports của controller mới.

### DB State (đã verify)
| Bảng | Trạng thái |
|------|-----------|
| `accessories` | ✅ Columns mới: brand, qty_in_stock, qty_minimum, price_cost, price_sell, supplier, note |
| `gift_items` | ✅ Tồn tại |
| `item_movements` | ✅ Tồn tại (với trigger update_item_stock) |
| `order_gifts` | ✅ Tồn tại (với trigger auto_movement_on_gift_issue) |
| Views | ✅ `v_accessory_stock_alert`, `v_gift_stock_alert` |

### Files Modified (quan trọng)
```
backend/src/
├── controllers/accessories.controller.js  ← VIẾT LẠI HOÀN TOÀN
├── controllers/gifts.controller.js         ← TẠO MỚI
├── routes/accessories.routes.js            ← FIX CRASH (toggleAccessory → bỏ)
└── routes/inventory.routes.js              ← THÊM accessories + gifts + order-gifts routes

frontend/src/
├── App.tsx                    ← thêm import + 2 routes mới
├── components/Layout.tsx       ← thêm 2 sidebar links
├── pages/AccessoriesPage.tsx   ← TẠO MỚI
└── pages/GiftsPage.tsx         ← TẠO MỚI

backend/migrations/
└── fix_accessories_and_gifts_v2.sql  ← đã chạy trên Supabase
```

---

## Next Steps (Priority)

1. **[NGAY] Restart backend**:
   ```bash
   cd backend
   # Ctrl+C dừng nodemon hiện tại
   npm run dev
   # Phải thấy: ✅ Supabase client khởi tạo thành công + Server đang chạy...
   ```

2. **[TEST] Verify endpoints hoạt động**:
   - Mở `http://localhost:3000/accessories` → phải load trang
   - Mở `http://localhost:3000/gifts` → phải load trang
   - Kiểm tra `PurchaseOrdersPage` → load đơn nhập hàng không còn 502

3. **[TIẾP] Tích hợp order_gifts vào SalesNewPage**:
   - Trong bước tạo đơn hàng, thêm section "Quà tặng kèm"
   - Gọi `POST /api/inventory/order-gifts/:order_id` để gắn quà vào đơn
   - Sau khi giao xe → warehouse gọi `PATCH /api/inventory/order-gifts/issue/:id`

4. **[SAU] Cân nhắc xóa route trùng lặp**:
   - Hiện tại `/api/accessories` và `/api/inventory/accessories` đều hoạt động
   - Dài hạn: migrate toàn bộ sang `/api/inventory/accessories` rồi xóa `accessories.routes.js`

---

## Important Notes

### API Endpoints đầy đủ sau fix

**Phụ kiện** (2 prefix giống nhau):
```
GET    /api/accessories              = /api/inventory/accessories
GET    /api/accessories/:id          = /api/inventory/accessories/:id
POST   /api/accessories              = /api/inventory/accessories
PUT    /api/accessories/:id          = /api/inventory/accessories/:id
POST   /api/accessories/:id/stock-in
POST   /api/accessories/:id/stock-out
POST   /api/accessories/:id/adjust   (kiểm kê)
GET    /api/accessories/:id/movements
GET    /api/accessories/low-stock
```

**Quà tặng** (chỉ `/api/inventory/`):
```
GET    /api/inventory/gift-items
GET    /api/inventory/gift-items/:id
POST   /api/inventory/gift-items
PUT    /api/inventory/gift-items/:id
POST   /api/inventory/gift-items/:id/stock-in
POST   /api/inventory/gift-items/:id/stock-out
GET    /api/inventory/gift-items/:id/movements
GET    /api/inventory/gift-items/low-stock
```

**Quà theo đơn hàng**:
```
GET    /api/inventory/order-gifts/:order_id
POST   /api/inventory/order-gifts/:order_id
PATCH  /api/inventory/order-gifts/issue/:id
```

### Supabase Connection
- Project ref: `zmqukcucgpjhmyfjcjvd`
- Không có direct DB access — chạy SQL qua Supabase Dashboard SQL Editor
- Backend dùng service_role key (bypass RLS)

### Pattern code
- Controller: follow `inventory.controller.js` (spare_parts pattern)
- Frontend: follow `SparePartsPage.tsx` (query/mutation/modal pattern)
- Tất cả text tiếng Việt

---

## Resources

- Supabase Dashboard: https://supabase.com/dashboard/project/zmqukcucgpjhmyfjcjvd
- Backend dev: `cd backend && npm run dev` (port 5000)
- Frontend dev: `cd frontend && npm run dev` (port 3000)
- Migration đã chạy: `backend/migrations/fix_accessories_and_gifts_v2.sql`
- Handoff cũ (context đầy đủ): `docs/handoff/handoff-20260330-1800.md`
